name: Publish

on:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  Publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ssh-key: ${{ secrets.COMMIT_KEY }}
      - name: Setup pnpm
        uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e # v6
        with:
          run_install: false
      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          cache: 'pnpm'
          node-version: '24.15.0'
          registry-url: https://registry.npmjs.org/
      - name: Install
        run: pnpm install
      - name: Bump versions
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          VERSION=${TAG#v}
          git config user.name mergewhether
          git config user.email bot@risto.io
          pnpm -r --filter './packages/*' exec npm version $VERSION --no-git-tag-version
          git add packages/config/package.json packages/mw-preset/package.json packages/mw-config/package.json
          git commit -m "$VERSION"
          git push origin HEAD:main
          git tag -f $TAG HEAD
          git push origin -f refs/tags/$TAG
      - name: Publish
        run: pnpm -r --filter './packages/*' publish --access public --no-git-checks --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
